<%= form_for(@conreview) do |f| %>
  <% if @conreview.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@conreview.errors.count, "error") %> prohibited this conreview from being saved:</h2>

      <ul>
      <% @conreview.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rating %><br>
    <%= f.number_field :rating %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  
  <div class="actions">
    <%= f.submit 'Submit Review' %>
  </div>
<% end %>
