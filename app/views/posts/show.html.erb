<div id = "bid_page">
<%= render(@post)%>
</div>
<% if @post.provider %>
<% if @post.consumer == current_user.consumer %>
<h1> This bid is won by <%=@post.provider.user.name%> </h1>
<br />
<br />
<% end %>
<% end %>


<% if @post.bids.length !=0 %>
	<table cellspacing = '0' id = "bid_table">
	<thead>
		<tr>
			<th>Bidder</th>
			<th>Amount</th>
			<th>Description</th>
			<th>Accept Bid</th>
		</tr>
	</thead>
	<% @post.bids.each do |bid| %>
	
	<tbody>
		<tr>
				<%if current_user.provider == bid.provider%> <td><%= current_user.name %><p>(you)</p> </td><td><%= number_to_currency(bid.amount) %></td> 
				<td><%= bid.description %></td><td></td>
		</tr>
		<tr>
				<% else %> <td><%= bid.provider.user.name %> </td><td><%= number_to_currency(bid.amount) %> </td><td><%= bid.description %></td>
				<% if (@show_accept) %>
					<% if(@post.provider.nil?)%>
						<td><%= link_to 'accept the bid', accept_bids_path(post_id: @post, bid_id: bid), :class => 'btn btn-primary btn-xs bidbtn' %></td></tr>
					<% end %>
				<% end %>
			<% end %> 
	<% end %>
	</tbody>
	</table>
<% end %>